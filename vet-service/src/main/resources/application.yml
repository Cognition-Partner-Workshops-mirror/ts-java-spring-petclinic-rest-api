spring:
  application:
    name: vet-service
  profiles:
    active: dev

server:
  port: 8080
  servlet:
    context-path: /petclinic/api

management:
  endpoints:
    web:
      exposure:
        include: health,info
