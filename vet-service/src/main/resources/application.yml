spring:
  application:
    name: vet-service
  profiles:
    active: dev
  jpa:
    open-in-view: false

server:
  port: 8081
  servlet:
    context-path: /

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
