spring:
  application:
    name: vet-service
  profiles:
    active: dev
  jpa:
    open-in-view: false

server:
  port: 8083
  servlet:
    context-path: /petclinic/api

management:
  endpoints:
    web:
      exposure:
        include: health,info
