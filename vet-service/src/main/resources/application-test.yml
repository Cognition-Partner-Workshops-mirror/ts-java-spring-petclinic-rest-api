# Test profile using H2 in-memory database without SQL logging
spring:
  datasource:
    url: jdbc:h2:mem:vetdb-test
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
